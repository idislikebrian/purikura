[build]
builder = "nixpacks"
buildCommand = "pnpm install && pnpm --filter @purikura/coordinator build"

[deploy]
startCommand = "node apps/coordinator/dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
